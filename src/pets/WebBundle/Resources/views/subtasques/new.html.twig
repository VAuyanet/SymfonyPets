{% extends 'base.html.twig' %}

{% block content %}
    <h1>Creació de Subtasca</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('subtasques_index') }}">Tornar a la llista</a>
        </li>
    </ul>
{% endblock %}
