{% extends 'base.html.twig' %}

{% block content %}
    <h1>Editar Usuari</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Editar usuari" class="btn" />
    {{ form_end(edit_form) }}

    <div>
        <a href="{{ path('usuaris_index') }}">Tornar a la llista</a>
    </div>
    <div>
        {{ form_start(delete_form) }}
            <input type="submit" value="Borrar usuari" class="btn">
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
