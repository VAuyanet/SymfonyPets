{% extends "base.html.twig" %}
{% block content %}
   {{form_start(form, {'action':'', 'method':'POST'})}}
    <h1>{{ titol }}</h1>
    {#{{ form }}#}
    
    {{form_end(form)}}
    
    {{ estat }}
</body>
</html>

{% endblock %}

