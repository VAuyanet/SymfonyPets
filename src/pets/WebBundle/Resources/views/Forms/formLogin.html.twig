{% include "base.html.twig" %}
{% block body%}
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-3">
            </div>
            <div class="col-sm-9">
                <h1>{{ titol }}</h1>
                {{ form_start(form, {'action':'', 'method':'POST'}) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}